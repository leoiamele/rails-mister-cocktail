<h1><%= @cocktail.name %></h1>

<ul>
  <% @cocktail.doses.each do |dose| %>
    <li>
      <%= dose.ingredient.name %> <%= dose.description %> | <%= link_to "Delete", dose_path(dose), method: :delete, data: { confirm: "Are you sure?"} %>
    </li>
  <% end %>
</ul>

<p>Ingredients:</p>
<%= render "cocktails/form_dose" %>

<%= link_to "Delete Cocktail", cocktail_path(@cocktail),
      method: :delete,
      data: { confirm: "Are you sure?" } %>

<%= link_to "See all Coacktails" , cocktails_path %>
